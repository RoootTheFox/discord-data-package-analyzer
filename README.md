# discord-data-package-analyzer

simple cli program to analyze a discord data package and get some stats from it

made because i was bored and wanted to do something in rust

usage: build and put the data dump in a folder called "package" in the same working directory that you are calling the program from
